- include: std


- py-func:
   name: build
   code: |
      import sys
      import traceback
      from actl import Buffer, Project

      while True:
         try:
            this['uinput'] = Buffer(input('>>> '))
         except EOFError:
            break
         except Exception: #pylint: disable=W0703
            traceback.print_exc()
            break
         this['std', 'build']()
