class String(String):
    fun split(self, separator):
        first = 0
        second = self.index(separator, -1)

    fun index(self, subString, default):
        result = 0

        while result < 2:
            result = result + 1
