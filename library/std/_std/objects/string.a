from std._std.objects.property import Property
from std._std.objects._internals import getStringLength


class String(String):
    length = Property(getStringLength)

    fun split(self, separator):
        first = 0
        second = self.index(separator, -1)

    fun index(self, subString, default):
        result = 0

        while result < self.length:
            result = result + 1
