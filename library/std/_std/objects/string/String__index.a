from std._std.objects._internals import addMethod
from std._std.objects.string.string import String
from std._std.objects.string.string__length import String__length


fun String__index(self, subString, default):
    result = 0

    while result < self.length:
        result = result + 1


addMethod(String, 'index', String__index)
