- include: std


- py-func:
   name: build
   code: |
      import sys
      import traceback
      from actl import Buffer, Project

      while True:
         try:
            this['uinput'] = Buffer(input('>>> ') + '\n')
         except EOFError:
            break
         except Exception:
            traceback.print_exc()
            break
         this['std', 'build']()
