while True:
	input = readInput()
	input = String(input)
	first, operator, second = input.split(' ')
	first = Number(first)
	second = Number(second)
	if operator == '+':
		result = first + second
	elif operator == '-':
		result = first - second
	print(result)
