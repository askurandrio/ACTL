while True:
	input = readInput()
	first, operator, second = input.split(' ')
	first = Number(first)
	second = Number(second)
	if operator == '+':
		result = first + second
	elif operator == '-':
		result = first - second
	print(result)
